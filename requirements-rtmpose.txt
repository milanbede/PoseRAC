# RTMPose extraction (scripts/rtmpose_extract.py, scripts/rtmpose_assets.py).
# Known-good interpreter: morning-forge tool/dataset_review/.venv (Python 3.12).
rtmlib==0.0.16
onnxruntime==1.22.1

# Training/eval (scripts/train_rtmpose.py, scripts/eval_rtmpose.py) run in the
# existing repo venv (poserac/): torch, pytorch_metric_learning, numpy.
# The official MediaPipe checkpoint is intentionally not used: native 17-joint
# features (34-dim) and the added jab class make it incompatible.
